# Virtual environment (rebuild from requirements.txt)
eeg_clean/

# Python cache
__pycache__/
*.py[cod]

# Editor
.vscode/

# MCP config with machine-local paths
.mcp.json

# Analysis outputs (kept outside git - PNG-heavy, regenerable)
results/
logs/

# Cohort overview cache (regenerated from Google Sheets MCP each run)
overview/.demographics_cache.json
overview/.exclusion_reasons_cache.json
